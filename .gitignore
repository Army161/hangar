# Runtime state — machine-specific, never shared
manifests/*.json
quarantine/

# Node
node_modules/
npm-debug.log*

# Rust / Tauri (from M2 onward)
target/
apps/desktop/src-tauri/target/

# OS
Thumbs.db
.DS_Store

# Secrets — Hangar flags plaintext credentials in argv; it must not ship any
.env
.env.*
*.pem
*.key

# Rust build output
target/
node_modules/
package-lock.json

# Secrets — API keys for optional integrations. Never commit.
config/settings.local.json
demo/out/
